@model ICollection<String>

@{
    ViewBag.Title = "Forsiden";
}

<div class="jumbotron">
    <h1>Velkommen til det nye Skema!, nu på github!</h1>
</div>

<div class="row">
    <div class="col-md-12">
        <h1>All the database items from the table test</h1>
        @foreach (var item in Model)
        {
            <div class="alert alert-danger">@item</div>
        }
    </div>
</div>